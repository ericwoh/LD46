
public enum JOBK // tag = jobk
{
    WarmHome,
    StoreFood,
    CollectFood,
    Build,
}
public enum RESOURCEK // tag = resk

{
    Food,
    WarmBed,
    Work, // time?
}

public enum NEEDK // tag = needk
{
    Food,
    Warmth,
    // lubrication?
}
